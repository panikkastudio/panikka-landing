import { MdxLayout } from "@/apps/use-case/mdx-components";

export const metadata = {
  author: "John Doe",
};

## The Problem:

In a world of endless selfies and snapshots, iPhone users found themselves drowning in digital clutter.

## Our Solution:

Neaten - the iOS app that turns photo management from a chore into a breeze.

## Key Features:

- Swipe-to-Delete: Intuitive gesture control for quick removal
- Smart Album Creation: Organize photos with a single tap
- Progress Tracker: Visual insights into your decluttering journey

## User Benefits:

- Maximized storage efficiency
- Enhanced photo discovery
- Streamlined camera roll management

### Privacy-First Approach:

Neaten processes all photos locally, ensuring user data never leaves the device.

## Result:

A cleaner, more manageable photo gallery that enhances the user's mobile experience."

export default function MDXPage({ children }) {
  return (
    <MdxLayout title="Neaten" tags={[1, 2, 3, 4]}>
      {children}
    </MdxLayout>
  );
}
